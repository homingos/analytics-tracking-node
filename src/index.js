import * as Defaults from './AnalyticsService';

export const { init } = Defaults;
export const { tagManager } = Defaults;
export const { adjustEvent } = Defaults;
export const { adjust } = Defaults;

export default {
  ...Defaults
};
